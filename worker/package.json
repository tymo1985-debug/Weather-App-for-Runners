{
  "private": true,
  "type": "module",
  "scripts": {
    "dry-run": "wrangler deploy --dry-run --outdir .wrangler-dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "web-push": "3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
